# loginpage
A simple login, register, and forget password page, with simple function of:
- check password strength. rules:
        + need more than 8 characters
        + special characters
        + include number characters
- hide and show password
- password confirm  
